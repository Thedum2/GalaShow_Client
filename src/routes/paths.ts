export const PATHS = {
    root: "/",
    welcome: "/",
    lobby: "/lobby",
    select: "/select",
    loading: "/loading",
    result: "/result",
    winner: "/winner",
    simulation_sample: "/simulation_sample",
} as const;
